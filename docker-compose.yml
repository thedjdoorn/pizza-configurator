version: "3.5"

services:
  pizza:
    build: .
    networks:
      - proxy
      - default
    container_name: pizza
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=proxy"
      - "traefik.http.routers.pizza.entrypoints=web"
      - "traefik.http.routers.pizza.rule=HOST(`pizza.${PWDOCKER_DOMAIN}`)`"
      - "traefik.http.routers.pizza.service=pizza"

networks:
  proxy:
    external: true
